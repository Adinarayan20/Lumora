/**
 * Universal metadata field data types supported across Lumora.
 */
export enum FieldType {
  STRING = "STRING",
  NUMBER = "NUMBER",
  BOOLEAN = "BOOLEAN",
  DATE = "DATE",
  ENUM = "ENUM",
  RELATIONSHIP = "RELATIONSHIP",
  FILE = "FILE",
  JSON = "JSON",
}
