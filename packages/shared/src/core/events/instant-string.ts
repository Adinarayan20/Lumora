/**
 * Semantic type alias representing an ISO-8601 UTC timestamp string.
 * Used across domain contracts to communicate temporal instant values without runtime parsing.
 */
export type InstantString = string;
